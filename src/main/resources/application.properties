spring.datasource.url=jdbc:mysql://${db.host}:${db.port}/${db.name}?createDatabaseIfNotExist=true
spring.datasource.username=${db.username}
spring.datasource.password=${db.password}
server.port=8080

spring.jpa.hibernate.ddl-auto=update
server.servlet.context-path=/api/v1.0

aws.access.key=${aws.access.key1}
aws.secret.key=${aws.secret.key1}
aws.region=ap-south-1
aws.bucket.name=billing-software-vardhan

jwt.secret.key=${jwt.secret.key1}

razorpay.key.id=${razorpay.key.id1}
razorpay.key.secret=${razorpay.key.secret1}